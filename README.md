### Almost Lossless Cut

Lossless cut engine based on ffmpeg implemented in rust.
In active development phase.

The idea is to copy as much content between keyframes as possible, reencode the rest and concatenate the results.
